language: objective-c
xcode_project: CleanroomBridging.xcodeproj
osx_image: xcode8

git:
  depth: 1
  submodules: false

before_install:
  - git submodule update --init

install:
  - gem install xcpretty

script:
- set -o pipefail && xcodebuild -project CleanroomBridging.xcodeproj -configuration Release ONLY_ACTIVE_ARCH=YES -scheme CleanroomBridging-iOS -destination 'platform=iOS Simulator,OS=10.0,name=iPhone 6s Plus' -destination-timeout 300 clean build | tee "CleanroomBridging-iOS-build.log" | xcpretty
- set -o pipefail && xcodebuild -project CleanroomBridging.xcodeproj -configuration Release ONLY_ACTIVE_ARCH=YES -scheme CleanroomBridging-iOS -destination 'platform=iOS Simulator,OS=10.0,name=iPhone 6s Plus' -destination-timeout 300 test | tee "CleanroomBridging-iOS-test.log" | xcpretty
- set -o pipefail && xcodebuild -project CleanroomBridging.xcodeproj -configuration Release ONLY_ACTIVE_ARCH=YES -scheme CleanroomBridging-OSX -destination 'platform=macOS' -destination-timeout 300 clean build | tee "CleanroomBridging-macOS-build.log" | xcpretty
- set -o pipefail && xcodebuild -project CleanroomBridging.xcodeproj -configuration Release ONLY_ACTIVE_ARCH=YES -scheme CleanroomBridging-OSX -destination 'platform=macOS' -destination-timeout 300 test | tee "CleanroomBridging-macOS-test.log" | xcpretty
- set -o pipefail && xcodebuild -project CleanroomBridging.xcodeproj -configuration Release ONLY_ACTIVE_ARCH=YES -scheme CleanroomBridging-tvOS -destination 'platform=tvOS Simulator,OS=10.0,name=Apple TV 1080p' -destination-timeout 300 clean build | tee "CleanroomBridging-tvOS-build.log" | xcpretty
- set -o pipefail && xcodebuild -project CleanroomBridging.xcodeproj -configuration Release ONLY_ACTIVE_ARCH=YES -scheme CleanroomBridging-tvOS -destination 'platform=tvOS Simulator,OS=10.0,name=Apple TV 1080p' -destination-timeout 300 test | tee "CleanroomBridging-tvOS-test.log" | xcpretty
- set -o pipefail && xcodebuild -project CleanroomBridging.xcodeproj -configuration Release ONLY_ACTIVE_ARCH=YES -scheme CleanroomBridging-watchOS -destination 'platform=watchOS Simulator,OS=3.0,name=Apple Watch - 42mm' -destination-timeout 300 clean build | tee "CleanroomBridging-watchOS-build.log" | xcpretty

after_failure:
- echo "Travis test result is $TRAVIS_TEST_RESULT"
- printf "\niOS build log:\n"
- cat "CleanroomBridging-iOS-build.log"
- printf "\niOS test log:\n"
- cat "CleanroomBridging-iOS-test.log"
- printf "\nmacOS build log:\n"
- cat "CleanroomBridging-macOS-build.log"
- printf "\nmacOS test log:\n"
- cat "CleanroomBridging-macOS-test.log"
- printf "\ntvOS build log:\n"
- cat "CleanroomBridging-tvOS-build.log"
- printf "\ntvOS test log:\n"
- cat "CleanroomBridging-tvOS-test.log"
- printf "\nwatchOS build log:\n"
- cat "CleanroomBridging-watchOS-build.log"

